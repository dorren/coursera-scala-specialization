val str = "007034,,,"
val list = str.split(",").toList

val a = List(1,2,3,4)
1.0 * a.sum / a.length
6371e3

10 until 1 by -2

import math._
import scala.reflect.io.Path
atan(sinh(Pi)) * 180.0 / Pi
atan(sinh(Pi)).toDegrees

val m1 = Map("a" -> 1, "b" -> 2).toIterable

List(List(5,6), List(4)).flatMap(x => x)
List(1,2,3,4,5).par.map(x => x + 10).toArray


val path = "temperatures/1975/0/0-0.png"
path.splitAt(path.lastIndexOf("/")+1)




